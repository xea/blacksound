/**
 * Provides types to support working with uniquely identified object instances. Any type may play the role of an unique
 * ID as long as equality is defined on them in a symmetric, reflective, transitive, null-intolerant and consistent manner.
 */
package so.blacklight.blacksound.id;